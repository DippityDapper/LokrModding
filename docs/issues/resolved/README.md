# Resolved issues

Fixed problems live here as one markdown file each. Do not delete them;
they are history for later regressions. In-game confirm is required
before a file arrives here from `unresolved/` or `unresolved-tested/`.
