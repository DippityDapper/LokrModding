# Unresolved issues

Open problems live here as one markdown file each. Process is in
[`../README.md`](../README.md). Do not move a file to `resolved/` until
the fix is confirmed in the running game. A passing unit test moves the
file to [`../unresolved-tested/`](../unresolved-tested/) instead.
